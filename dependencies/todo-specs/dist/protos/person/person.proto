// Code generated by furo-proto-gen. DO NOT EDIT.
syntax = "proto3";
package person;
option go_package = "github.com/theNorstroem/todo-specs/dist/pb/person;personpb";
option java_multiple_files = true;
option java_outer_classname = "PersonProto";
option java_package = "com.furo.baseperson";

import "furo/furo.proto";


// Someone has to do the work.
message Person {  

    // id (is a ULID). We do not use the email, they change sometimes.
    string id = 1;

    // We use this field label the person, using last_name and first_name is so 2008
    string display_name = 2;

    // Add some notes, limited to 140 chars
    string note = 3;

    // We should be able to contact someone.
    string email = 4;

    // We should be able to talk with someone.
    string mobile = 5;
}

// Collectioncontainer which holds a person.Person
message PersonCollection {  

    // the data contains a person.Person
    repeated person.PersonEntity entities = 1;

    // the Hateoas links
    repeated furo.Link links = 2;

    // Meta for the response
    furo.Meta meta = 3;
}

// Entitycontainer which holds a person.Person
message PersonEntity {  

    // the data contains a person.Person
    person.Person data = 1;

    // the Hateoas links
    repeated furo.Link links = 2;

    // Meta for the response
    furo.Meta meta = 3;
}

// Use this type to set a reference to a person
message Reference {  

    // Id of the referenced person.
    string id = 1;

    // Label of the referenced person
    string display_name = 2;

    // HTS for the initial search (works on root resources only)
    furo.Link link = 3;
}